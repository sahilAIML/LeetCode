class Solution {
    public String reverseWords(String s) {
        String ans = "";

        String[] word = s.trim().split("\\s+");

        for(String w : word){
            ans = w + " " + ans;
        }

        return ans.trim();
    }
}
